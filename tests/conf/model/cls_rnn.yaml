model:
  class: RNNModel
  module: experimenter.Classification.modeling
  params:
    dropout: 0
    embedding_dim: 20
    hidden_dim: 40
    num_classes: 2
    num_layers: 1
    max_seq_len:
        eval: 1
        value: "config['processor']['params']['seq_len']['inp'][0]"
